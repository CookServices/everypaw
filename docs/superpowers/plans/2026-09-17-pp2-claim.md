# PP-2, hommages en attente et réclamation — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Le créateur d'une page publique la réclame en s'inscrivant, et retrouve dans son compte l'animal, ses souvenirs en entrées, le chapitre, et les hommages déposés en son absence.

**Architecture:** Toute l'écriture de la réclamation vit dans une fonction Postgres `SECURITY DEFINER` appelée par une seule route en service role, pour que les six écritures soient une transaction et non six requêtes qui peuvent s'arrêter au milieu. La fonction est révoquée pour les rôles clients. Le reste est du branchement : les hommages acceptent une page au lieu d'un animal, la page publique annonce leur nombre sans les montrer, et le tableau de bord rattrape le cas où la session revient sans le paramètre d'URL.

**Tech Stack:** Next.js 14 App Router, TypeScript, Supabase (service role, plpgsql), Vitest, styles inline.

**Spec:** `docs/acquisition/specs.md`, section PP-2.

## Global Constraints

- **Une PR pour tout PP-2**, branche `feat/pp2-claim`. `main` refuse tout push direct.
- **Definition of Done** (`CLAUDE.md`) : `npx tsc --noEmit` sans **nouvelle** erreur, `npm test` vert, `npm run build` passé, clés i18n dans les deux fichiers, rendu ouvert dans un navigateur.
- **Une erreur `tsc` préexiste sur `main`** : `src/app/api/stripe/webhook/route.test.ts(28,92): error TS2556`. Elle n'est pas de ce chantier. Vérifier seulement qu'il n'y en a pas une deuxième.
- **Ne jamais lancer `npm run build` pendant qu'un `next dev` tourne** (même `.next/`).
- **Le français vouvoie.** `src/lib/copy-register.test.ts` parcourt tout `messages/fr.json`. Sa liste `ADDRESSED_TO_THE_PET` contient déjà `memorial_landing.example_output` ; n'y ajouter une clé que si c'est l'animal qui parle.
- **Aucune flèche (`→`, `->`) ni tiret cadratin (`—`) dans la copie visible.**
- **Tous les styles inline**, hover via `onMouseEnter`/`onMouseLeave`. Tokens exacts : fond sombre `#1C1410`, encre claire `#F7F2EA`, accent `#C8813A`, fond clair `#F7F2EA`, encre `#3D2B1F`, doux `#7A5C44`, estompé `#9A8070`. Titres `Georgia, serif`, corps `'DM Sans', sans-serif`, boutons en pilule radius 100.
- **Jamais `createClient` de `@supabase/supabase-js` dans une route** : `getServiceSupabase()` de `@/lib/supabase/service`.
- **Jamais de détail d'erreur interne renvoyé au client.** Logger via `log.error`, rendre un code générique.
- **Le jeton de réclamation ne voyage jamais dans une URL.** Il vit dans `localStorage` sous `ep_claim_{slug}` et dans le corps des requêtes.
- **Toute RPC qui écrit est révoquée pour `anon` et `authenticated`** et accordée au seul `service_role`. C'est la leçon de l'audit du 2026-07-06, où des RPC de crédits laissées ouvertes permettaient à tout compte de s'auto-créditer des livres.
- **Migrations idempotentes**, `IF NOT EXISTS` ou blocs `DO $$ … $$`, nommées `<description>_2026_09_17.sql`, appliquées à la main dans l'éditeur SQL Supabase avant le merge.

## Ce que l'audit a trouvé et que la spec ignore

Trois obstacles réels, vérifiés dans le schéma de production le 2026-09-17. Les tâches ci-dessous les traitent ; ne pas les redécouvrir.

1. **Un déclencheur bloquera la réclamation d'un utilisateur gratuit.** `trg_enforce_free_entry_limit` est un `BEFORE INSERT` sur `entries` qui lève `entry_limit` dès que le compte gratuit atteint dix entrées sur ses propres animaux. La réclamation en insère jusqu'à trois. La spec dit explicitement que le plan gratuit doit pouvoir réclamer, donc le déclencheur doit connaître une exemption. Tâche 1.
2. **`memorial_tributes.pet_id` est `NOT NULL REFERENCES pets(id)`.** Il faut lui retirer le `NOT NULL`, ajouter `page_id`, et poser une contrainte qui exige l'un ou l'autre. Tâche 1.
3. **Les deux policies RLS de `memorial_tributes` joignent `pets` sur `pet_id`.** Avec `pet_id` nul, la sous-requête ne rend rien, donc un hommage de page n'est lisible par aucun client. C'est le comportement voulu, pas un bug, mais la tâche 1 doit le vérifier plutôt que le supposer.

---

## File Structure

| Fichier | Responsabilité |
|---|---|
| `supabase/migrations/add_tribute_pages_and_claim_2026_09_17.sql` (créer) | Colonnes et contrainte sur `memorial_tributes`, exemption du déclencheur d'entrées, fonction `claim_public_page`, révocations. |
| `src/app/api/memorial/tributes/route.ts` (modifier) | Accepte `pageId` en plus de `petId`. Aucun email quand la cible est une page. |
| `src/app/api/public-pages/claim/route.ts` (créer) | Session requise, hachage du jeton, appel du RPC, traduction du résultat en statut HTTP. |
| `src/app/p/[slug]/page.tsx` (modifier) | Redirection 308 si réclamée, compte d'hommages en attente, formulaire d'hommage, bouton de réclamation. |
| `src/components/public-page/PublicPageActions.tsx` (modifier) | Le bouton de réclamation appelle la route quand une session existe, sinon envoie vers l'inscription. |
| `src/components/public-page/PublicPageTributes.tsx` (créer) | Formulaire d'hommage pour une page, plus la ligne qui annonce le nombre en attente. |
| `src/components/dashboard/ClaimBanner.tsx` (créer) | Rattrapage : lit `localStorage`, propose la réclamation d'une page encore active. |
| `src/app/dashboard/page.tsx` (modifier) | Monte le bandeau de rattrapage. |
| `messages/en.json`, `messages/fr.json` (modifier) | Section `public_page` enrichie. |
| `src/app/api/generate/route.ts`, `src/lib/story.ts`, `src/app/dashboard/pets/[id]/page.tsx` (modifier) | `memorial` exclu du quota d'histoires. |
| `CLAUDE.md`, `docs/acquisition/specs.md` (modifier) | Schéma, routes, session. |

---

### Task 1: Migration et fonction de réclamation

**Files:**
- Create: `supabase/migrations/add_tribute_pages_and_claim_2026_09_17.sql`

**Interfaces:**
- Consomme : les tables `public_pages`, `pets`, `entries`, `stories`, `profiles`, `memorial_tributes` telles qu'elles existent.
- Produit : `claim_public_page(p_slug text, p_token_hash text, p_user uuid) returns jsonb`. Le résultat est `{"ok": true, "pet_id": "<uuid>"}`, ou `{"ok": false, "reason": "not_claimable"}`, ou `{"ok": false, "reason": "bad_token"}`. Les deux échecs surviennent **avant toute écriture**. Produit aussi `memorial_tributes.page_id`.

- [ ] **Step 1: Écrire la migration**

Créer le fichier avec ce contenu exact :

```sql
-- Réclamation d'une page créée sans compte (docs/acquisition/specs.md, PP-2).
-- Idempotent.

-- ── 1. Un hommage peut viser une page publique au lieu d'un animal ──────────
alter table memorial_tributes add column if not exists page_id uuid
  references public_pages(id) on delete cascade;

alter table memorial_tributes alter column pet_id drop not null;

-- Un hommage vise exactement l'un des deux. La réclamation fait passer une
-- ligne de la page à l'animal, donc les deux peuvent être remplis un instant ;
-- la contrainte n'exige que la présence d'au moins une cible.
do $$
begin
  if not exists (
    select 1 from pg_constraint where conname = 'memorial_tributes_target_check'
  ) then
    alter table memorial_tributes add constraint memorial_tributes_target_check
      check (pet_id is not null or page_id is not null);
  end if;
end $$;

create index if not exists memorial_tributes_page_id_idx
  on memorial_tributes (page_id) where page_id is not null;

-- ── 2. La réclamation échappe au plafond d'entrées du plan gratuit ─────────
-- Le corps ci-dessous est celui de restore_contributor_entry_exemption_2026_07_06.sql,
-- augmenté d'une seule sortie anticipée. Refuser les souvenirs d'un visiteur
-- parce que son compte gratuit est déjà à dix entrées reviendrait à perdre la
-- page qu'il vient d'écrire, au moment précis où il s'inscrit pour la garder.
-- Le drapeau est local à la transaction et aucun client ne peut le poser :
-- PostgREST n'expose pas set_config, et la seule fonction qui l'appelle est
-- révoquée pour anon et authenticated.
create or replace function public.enforce_free_entry_limit()
returns trigger
language plpgsql
security definer
set search_path = public
as $$
DECLARE
  v_plan      text;
  v_count     int;
  v_pet_owner uuid;
BEGIN
  IF coalesce(current_setting('everypaw.claiming', true), '') = '1' THEN
    RETURN new;
  END IF;

  -- Contributor entries (adding to a pet the user does not own) never count.
  SELECT user_id INTO v_pet_owner FROM public.pets WHERE id = new.pet_id;
  IF v_pet_owner IS DISTINCT FROM new.user_id THEN
    RETURN new;
  END IF;

  SELECT plan INTO v_plan FROM public.profiles WHERE id = new.user_id;

  -- Only the free plan is capped; a missing profile is treated as free.
  IF coalesce(v_plan, 'free') <> 'free' THEN
    RETURN new;
  END IF;

  SELECT count(*) INTO v_count FROM public.entries WHERE user_id = new.user_id;

  -- Message 'entry_limit' is matched by the client to show the upsell.
  IF v_count >= 10 THEN
    RAISE EXCEPTION 'entry_limit' USING errcode = 'check_violation';
  END IF;

  RETURN new;
END;
$$;

-- ── 3. La réclamation elle-même ────────────────────────────────────────────
create or replace function public.claim_public_page(
  p_slug text,
  p_token_hash text,
  p_user uuid
)
returns jsonb
language plpgsql
security definer
set search_path = public
as $$
declare
  v_page      public_pages%rowtype;
  v_pet_id    uuid;
  v_entry     date;
  v_memory    text;
  v_message   text;
  v_type      text;
begin
  -- Verrou sur la ligne : deux réclamations simultanées se suivent au lieu de
  -- créer deux animaux. La seconde trouvera status <> 'active'.
  select * into v_page
  from public.public_pages
  where slug = p_slug and status = 'active'
  for update;

  if not found then
    return jsonb_build_object('ok', false, 'reason', 'not_claimable');
  end if;

  -- Comparaison de deux chaînes hexadécimales de longueur fixe. Elle n'est pas
  -- à temps constant, mais l'attaquant devrait distinguer des écarts de
  -- nanosecondes à travers un aller-retour réseau et une requête SQL, pour un
  -- secret de 256 bits. Le jeton brut n'est jamais stocké ni comparé.
  if v_page.claim_token_hash is distinct from p_token_hash then
    return jsonb_build_object('ok', false, 'reason', 'bad_token');
  end if;

  -- À partir d'ici on écrit. Tout échec annule l'ensemble.
  perform set_config('everypaw.claiming', '1', true);

  insert into public.pets (user_id, name, species, birthdate, photo_url,
                           deceased_at, memorial_photo_url, memorial_message)
  values (
    p_user,
    v_page.pet_name,
    v_page.species,
    v_page.birthdate,
    v_page.photo_url,
    v_page.deceased_at,
    case when v_page.kind = 'memorial' then v_page.photo_url else null end,
    case when v_page.kind = 'memorial'
         then left(split_part(v_page.story_content, '.', 1), 300) || '.'
         else null end
  )
  returning id into v_pet_id;

  -- Les souvenirs deviennent des entrées. Pour un mémorial elles sont datées
  -- de la veille du départ, faute de mieux : ce sont des souvenirs d'avant.
  v_entry := case when v_page.deceased_at is not null
                  then v_page.deceased_at - 1
                  else current_date end;

  for v_memory in select jsonb_array_elements_text(v_page.memories)
  loop
    insert into public.entries (pet_id, user_id, content, entry_date)
    values (v_pet_id, p_user, v_memory, v_entry);
  end loop;

  v_type := case when v_page.kind = 'memorial' then 'memorial' else 'origins' end;

  insert into public.stories (pet_id, user_id, title, content, status,
                              story_type, period_start, period_end)
  values (v_pet_id, p_user, v_page.story_title, v_page.story_content,
          'published', v_type, v_entry, v_entry);

  -- Les hommages déposés pendant que la page était anonyme rejoignent l'animal,
  -- en attente : le nouveau propriétaire les modère dans l'onglet existant.
  update public.memorial_tributes
  set pet_id = v_pet_id
  where page_id = v_page.id;

  update public.public_pages
  set status = 'claimed',
      claimed_by = p_user,
      claimed_pet_id = v_pet_id,
      claimed_at = now()
  where id = v_page.id;

  -- L'animal existe, donc l'onboarding n'a plus de raison de s'afficher.
  update public.profiles
  set onboarding_completed = true,
      onboarding_dismissed = true
  where id = p_user;

  return jsonb_build_object('ok', true, 'pet_id', v_pet_id);
end;
$$;

-- Jamais exposée aux clients : elle écrit dans pets, entries, stories et
-- profiles au nom d'un utilisateur passé en paramètre. Laissée ouverte, tout
-- JWT pourrait réclamer la page d'un autre en devinant un slug.
revoke execute on function public.claim_public_page(text, text, uuid) from public, anon, authenticated;
grant execute on function public.claim_public_page(text, text, uuid) to service_role;
```

- [ ] **Step 2: Appliquer la migration**

La coller dans l'éditeur SQL Supabase et lancer. Si le propriétaire du dépôt n'est pas disponible, **s'arrêter et le signaler** : les tâches suivantes ne peuvent rien vérifier sans elle.

- [ ] **Step 3: Vérifier la forme, sans passer par l'application**

Avec un script Node lancé depuis la racine du dépôt, en service role, vérifier dans cet ordre :

1. `select id, pet_id, page_id from memorial_tributes limit 1` ne renvoie plus d'erreur de colonne inconnue.
2. Un appel `rpc('claim_public_page', { p_slug: 'inexistant', p_token_hash: 'x', p_user: '<un uuid quelconque>' })` rend `{ ok: false, reason: 'not_claimable' }` sans rien écrire.
3. La même RPC appelée avec la **clé anon** rend une erreur de permission. C'est la vérification qui compte le plus de cette tâche.

Écrire les trois résultats dans le rapport.

- [ ] **Step 4: Vérifier que les policies d'hommage tolèrent un `pet_id` nul**

Insérer en service role un hommage avec `page_id` renseigné et `pet_id` nul, sur une page jetable créée pour l'occasion. Confirmer que l'insertion passe, puis qu'une lecture avec la **clé anon** ne le rend pas. Supprimer la page et l'hommage ensuite. Rapporter ce qui a été observé.

- [ ] **Step 5: Commit**

```bash
git add supabase/migrations/add_tribute_pages_and_claim_2026_09_17.sql
git commit -m "feat(public-pages): migration et fonction de reclamation transactionnelle"
```

---

### Task 2: Les hommages acceptent une page

**Files:**
- Modify: `src/app/api/memorial/tributes/route.ts`

**Interfaces:**
- Consomme : `memorial_tributes.page_id` de la tâche 1.
- Produit : `POST /api/memorial/tributes` accepte `{ pageId }` **ou** `{ petId }`. `GET ?pageId=<uuid>` rend `{ pendingCount: number }` et jamais le contenu des hommages.

- [ ] **Step 1: Étendre le POST**

Dans `POST`, après le honeypot et avant la validation actuelle du `petId` :
- accepter `pageId`, validé par `UUID_REGEX`, exclusif avec `petId` (les deux fournis ou aucun des deux : `400 invalid_target`) ;
- quand c'est une page, vérifier qu'elle existe, que `status = 'active'` et que `kind = 'memorial'` ; sinon `404 not_found` ;
- insérer avec `page_id` renseigné et `pet_id` nul, `status: 'pending'` comme aujourd'hui ;
- **ne pas envoyer d'email** : la page n'a pas de propriétaire. Tout le bloc de notification reste réservé au cas `petId`.

Garder le limiteur existant tel quel : `checkRateLimitDb('tribute_submit_' + ip, 3, 60 * 60 * 1000)` couvre les deux cibles.

- [ ] **Step 2: Ajouter le compteur au GET**

Dans `GET`, avant la logique actuelle : si `pageId` est fourni et valide, rendre uniquement `{ pendingCount }`, obtenu par un `select` avec `{ count: "exact", head: true }` sur `page_id` et `status = 'pending'`. Ne jamais rendre `author_name` ni `message` pour une page : ils sont le motif de réclamer, pas un contenu public.

- [ ] **Step 3: Vérifier**

Serveur de dev lancé, port lu dans sa sortie. Créer une page jetable via `POST /api/public-pages`, puis :

```bash
curl -s -X POST http://localhost:PORT/api/memorial/tributes -H 'content-type: application/json' \
  -d '{"pageId":"<id>","authorName":"Marie","message":"Je pense a vous, il etait adorable."}'
curl -s "http://localhost:PORT/api/memorial/tributes?pageId=<id>"
```

Attendu : `{"ok":true}` puis `{"pendingCount":1}`. Vérifier en base que la ligne a `pet_id` nul. Vérifier qu'aucun email n'est parti : la sortie du serveur ne doit contenir aucune trace de `sendEmail` pour cette requête.

Vérifier aussi les refus : les deux cibles à la fois, aucune cible, un `pageId` d'une page inexistante.

- [ ] **Step 4: Commit**

```bash
git add src/app/api/memorial/tributes/route.ts
git commit -m "feat(tributes): un hommage peut viser une page sans compte"
```

---

### Task 3: La route de réclamation

**Files:**
- Create: `src/app/api/public-pages/claim/route.ts`

**Interfaces:**
- Consomme : `claim_public_page` de la tâche 1 ; `createClient` de `@/lib/supabase/server` pour la session ; `getServiceSupabase` pour l'appel RPC.
- Produit : `POST /api/public-pages/claim` avec `{ slug, claimToken }`. Rend `200 { petId }`, ou `401 unauthorized`, `400 invalid_input`, `403 bad_token`, `409 not_claimable`, `500 claim_failed`.

- [ ] **Step 1: Écrire la route**

```ts
import { NextResponse } from "next/server";
import { createHash } from "node:crypto";
import { createClient as createServerClient } from "@/lib/supabase/server";
import { getServiceSupabase } from "@/lib/supabase/service";
import { checkRateLimitDb, getClientIp } from "@/lib/rate-limit";
import { log } from "@/lib/log";

const SLUG_REGEX = /^[0-9A-Za-z]{10}$/;
const TOKEN_REGEX = /^[0-9a-f]{64}$/;

export async function POST(req: Request) {
  // Un jeton de 256 bits ne se devine pas, mais rien n'empêche d'essayer en
  // boucle : ce plafond rend la tentative inutile sans gêner un vrai créateur,
  // qui ne réclame qu'une poignée de pages.
  const { allowed } = await checkRateLimitDb(`claim:${getClientIp(req)}`, 20, 60 * 60 * 1000);
  if (!allowed) return NextResponse.json({ error: "rate_limited" }, { status: 429 });

  const supabaseAuth = await createServerClient();
  const { data: { user } } = await supabaseAuth.auth.getUser();
  if (!user) return NextResponse.json({ error: "unauthorized" }, { status: 401 });

  let body: { slug?: unknown; claimToken?: unknown };
  try {
    body = await req.json();
  } catch {
    return NextResponse.json({ error: "invalid_input" }, { status: 400 });
  }

  const { slug, claimToken } = body;
  if (typeof slug !== "string" || !SLUG_REGEX.test(slug)) {
    return NextResponse.json({ error: "invalid_input" }, { status: 400 });
  }
  if (typeof claimToken !== "string" || !TOKEN_REGEX.test(claimToken)) {
    return NextResponse.json({ error: "invalid_input" }, { status: 400 });
  }

  // Le jeton brut ne quitte jamais cette fonction : seule son empreinte part
  // en base, où seule l'empreinte est stockée.
  const tokenHash = createHash("sha256").update(claimToken).digest("hex");

  const { data, error } = await getServiceSupabase().rpc("claim_public_page", {
    p_slug: slug,
    p_token_hash: tokenHash,
    p_user: user.id,
  });

  if (error) {
    log.error("[public-pages/claim] rpc failed:", error.message);
    return NextResponse.json({ error: "claim_failed" }, { status: 500 });
  }

  const result = data as { ok: boolean; reason?: string; pet_id?: string };
  if (!result?.ok) {
    if (result?.reason === "bad_token") {
      return NextResponse.json({ error: "bad_token" }, { status: 403 });
    }
    return NextResponse.json({ error: "not_claimable" }, { status: 409 });
  }

  return NextResponse.json({ petId: result.pet_id });
}
```

- [ ] **Step 2: Vérifier la compilation**

`npx tsc --noEmit`, seule l'erreur préexistante.

- [ ] **Step 3: Vérifier sans session**

```bash
curl -s -o /dev/null -w "%{http_code}\n" -X POST http://localhost:PORT/api/public-pages/claim \
  -H 'content-type: application/json' -d '{"slug":"AAAAAAAAAA","claimToken":"'"$(printf 'a%.0s' {1..64})"'"}'
```

Attendu : `401`. C'est la garde la plus importante de la route et elle se vérifie sans navigateur.

- [ ] **Step 4: Commit**

```bash
git add src/app/api/public-pages/claim/route.ts
git commit -m "feat(public-pages): route de reclamation, session requise"
```

---

### Task 4: Copie bilingue

**Files:**
- Modify: `messages/en.json`, `messages/fr.json`

**Interfaces:**
- Produit : les clés lues par les tâches 5 et 6, dans la section `public_page` existante.

- [ ] **Step 1: Ajouter les clés**

Anglais, dans `public_page` :

```json
"tributes_pending_one": "One person has left a tribute. Claim the page to read it and publish it.",
"tributes_pending_many": "{count} people have left tributes. Claim the page to read them and publish them.",
"tribute_form_title": "Leave a tribute",
"tribute_name_label": "Your name",
"tribute_message_label": "Your message",
"tribute_submit": "Leave it",
"tribute_sending": "Sending",
"tribute_done": "Thank you. Your tribute is waiting for the family.",
"tribute_error": "This tribute could not be sent. Please try again.",
"claiming": "Adding it to your account",
"claim_error": "This page could not be added to your account. Please try again.",
"claim_error_token": "This browser is not the one that created the page, so it cannot claim it.",
"claim_error_taken": "This page has already been added to an account.",
"claim_banner_title": "You created the page for {name}",
"claim_banner_cta": "Add it to my account"
```

Français, mêmes clés, vouvoiement :

```json
"tributes_pending_one": "Une personne a laissé un hommage. Réclamez la page pour le lire et le publier.",
"tributes_pending_many": "{count} personnes ont laissé un hommage. Réclamez la page pour les lire et les publier.",
"tribute_form_title": "Laisser un hommage",
"tribute_name_label": "Votre nom",
"tribute_message_label": "Votre message",
"tribute_submit": "Déposer",
"tribute_sending": "Envoi",
"tribute_done": "Merci. Votre hommage attend la famille.",
"tribute_error": "Cet hommage n'a pas pu être envoyé. Réessayez.",
"claiming": "Ajout à votre compte",
"claim_error": "Cette page n'a pas pu être ajoutée à votre compte. Réessayez.",
"claim_error_token": "Ce navigateur n'est pas celui qui a créé la page, il ne peut pas la réclamer.",
"claim_error_taken": "Cette page a déjà été ajoutée à un compte.",
"claim_banner_title": "Vous avez créé la page de {name}",
"claim_banner_cta": "L'ajouter à mon compte"
```

- [ ] **Step 2: Vérifier**

```bash
node -e "const a=Object.keys(require('./messages/en.json').public_page).sort(),b=Object.keys(require('./messages/fr.json').public_page).sort();console.log(JSON.stringify(a)===JSON.stringify(b)?'PARITE OK '+a.length:'DIFF')"
npx vitest run src/lib/copy-register.test.ts
```

Attendu : parité, et le test de registre vert sans ajouter d'entrée à `ADDRESSED_TO_THE_PET` (aucune de ces chaînes n'est l'animal qui parle).

Vérifier aussi l'absence de flèche et de tiret cadratin dans les deux blocs.

- [ ] **Step 3: Commit**

```bash
git add messages/en.json messages/fr.json
git commit -m "feat(public-pages): copie des hommages et de la reclamation"
```

---

### Task 5: La page publique gagne hommages et réclamation

**Files:**
- Modify: `src/app/p/[slug]/page.tsx`
- Modify: `src/components/public-page/PublicPageActions.tsx`
- Create: `src/components/public-page/PublicPageTributes.tsx`

**Interfaces:**
- Consomme : la route d'hommages (tâche 2), la route de réclamation (tâche 3), les clés i18n (tâche 4).
- Produit : la page rend la redirection, le compteur et les deux formulaires.

- [ ] **Step 1: Rediriger une page réclamée**

Dans `src/app/p/[slug]/page.tsx`, après le chargement et **avant** `isVisible`, si `page.status === 'claimed'` et `page.claimed_pet_id` est renseigné et `page.kind === 'memorial'`, appeler `redirect(\`/memorial/\${page.claimed_pet_id}\`, RedirectType.replace)` avec un 308. Utiliser `permanentRedirect` de `next/navigation`, qui rend exactement 308.

Le lien partagé continue donc de fonctionner après la réclamation. Une page `living` réclamée reste affichée telle quelle, c'est un instantané (PP-4).

`generateMetadata` doit faire la même chose ou, plus simple, laisser la page rediriger : vérifier que `generateMetadata` ne casse pas sur une page réclamée.

- [ ] **Step 2: Compter les hommages en attente**

Toujours en composant serveur, en service role, compter les `memorial_tributes` de cette page avec `status = 'pending'`. Passer le nombre au composant client.

- [ ] **Step 3: Écrire `PublicPageTributes`**

Composant client, props `{ pageId, pendingCount, locale }`. Il affiche :
- la ligne d'annonce quand `pendingCount > 0`, avec `tributes_pending_one` pour un, sinon `tributes_pending_many` dont `{count}` est remplacé ;
- un formulaire repliable : nom (1 à 100 caractères), message (1 à 1000), un honeypot `website` hors écran comme dans le formulaire de création, un bouton ;
- après succès, remplacer le formulaire par `tribute_done` et incrémenter le compte affiché localement.

Style sombre, mêmes tokens que la page. Le formulaire poste vers `/api/memorial/tributes` avec `{ pageId, authorName, message, website }`.

- [ ] **Step 4: Câbler le bouton de réclamation**

Dans `PublicPageActions`, le bouton actuel est un lien vers l'inscription. Il devient :
- si `localStorage` porte `ep_claim_{slug}`, un bouton qui poste vers `/api/public-pages/claim` avec le slug et le jeton, affiche `claiming` pendant l'appel, puis redirige vers `/dashboard/pets/{petId}` ;
- si la réponse est `401`, rediriger vers `/auth/signup?next=/p/{slug}?claim=1` : la session manque, l'inscription revient ici ;
- si la réponse est `403`, afficher `claim_error_token` ; si `409`, `claim_error_taken` ; sinon `claim_error` ;
- s'il n'y a pas de jeton dans ce navigateur, garder le lien vers l'inscription tel qu'il est aujourd'hui.

Si l'URL porte `?claim=1` et qu'un jeton existe, déclencher la réclamation au chargement, sans clic : c'est le retour d'inscription.

- [ ] **Step 5: Vérifier dans un navigateur**

Parcours complet, serveur de dev, port réel :
1. Créer une page depuis `/fr/memorial/new`.
2. Depuis un **autre** navigateur ou une fenêtre privée, ouvrir la page, déposer deux hommages. Vérifier que le texte des hommages n'apparaît nulle part et que le compteur annonce deux.
3. Revenir sur le navigateur créateur, se connecter avec un compte de test, cliquer « Réclamer ».
4. Vérifier l'arrivée sur `/dashboard/pets/<id>`, l'animal, trois entrées, le chapitre dans l'onglet Histoires, deux hommages dans l'onglet Hommages.
5. Rouvrir `/p/<slug>` : elle doit rediriger en 308 vers `/memorial/<petId>`.

Rapporter chaque étape avec ce qui a été observé.

- [ ] **Step 6: Commit**

```bash
git add src/app/p src/components/public-page
git commit -m "feat(public-pages): hommages en attente et reclamation sur la page"
```

---

### Task 6: Rattrapage au tableau de bord, quota, documentation

**Files:**
- Create: `src/components/dashboard/ClaimBanner.tsx`
- Modify: `src/app/dashboard/page.tsx`
- Modify: `src/app/api/generate/route.ts`, `src/lib/story.ts`, `src/app/dashboard/pets/[id]/page.tsx`
- Modify: `CLAUDE.md`, `docs/acquisition/specs.md`

- [ ] **Step 1: Exclure `memorial` du quota d'histoires**

Trois occurrences exactes de `.not("story_type", "in", "(origins,birthday)")`, vérifiées le 2026-09-16 :
- `src/app/api/generate/route.ts`
- `src/lib/story.ts`, dans `shouldShowFirstStoryNudge`
- `src/app/dashboard/pets/[id]/page.tsx`

Chacune devient `(origins,birthday,memorial)`. Un chapitre offert avec la page ne doit pas consommer l'unique génération du plan gratuit.

- [ ] **Step 2: Écrire le bandeau de rattrapage**

`ClaimBanner`, composant client. Au montage, lire toutes les clés `ep_claim_*` de `localStorage`. Pour chacune, demander `/api/public-pages/claim` ? Non : d'abord vérifier que la page est encore réclamable, avec un `GET /p/...` ce serait lourd. Le plus simple et le plus sûr : tenter la réclamation seulement sur clic, et n'afficher le bandeau que si au moins une clé existe. Le nom de l'animal n'étant pas dans `localStorage`, afficher `claim_banner_title` avec le slug remplacé par un libellé générique n'est pas satisfaisant : **stocker aussi le nom** au moment de la création.

Donc modifier aussi `PublicPageForm` pour écrire `localStorage['ep_claim_' + slug] = JSON.stringify({ token, name })`, et faire lire cette forme à `PublicPageActions` **et** au bandeau. Gérer l'ancienne forme (une chaîne brute) en repli, pour ne pas casser les pages créées avant ce déploiement.

Au clic, poster vers la route de réclamation. En cas de succès, retirer la clé et aller sur l'animal. En cas de `409`, retirer la clé sans rien dire : la page est déjà réclamée.

- [ ] **Step 3: Monter le bandeau**

Dans `src/app/dashboard/page.tsx`, rendre `ClaimBanner` en haut de la page, avant les autres encarts. Il ne rend rien s'il n'y a aucune clé.

- [ ] **Step 4: Vérifier que `OriginsFlow` ne se relance pas**

Sur le compte de test qui vient de réclamer une page, ouvrir `/dashboard`. `OriginsFlow` ne doit pas s'afficher : `hasOrigins` est calculé depuis les histoires existantes. Pour une page mémorial, l'histoire est de type `memorial` et non `origins`, donc `hasOrigins` sera faux et le flux s'ouvrira. **Vérifier ce point précis et rapporter.** S'il s'ouvre, la correction est de traiter un animal décédé comme non éligible à `OriginsFlow`, ce qui est de toute façon juste : on ne demande pas à quelqu'un qui vient d'enterrer son animal de raconter son arrivée.

- [ ] **Step 5: Documentation**

Dans `CLAUDE.md` : ajouter `/api/public-pages/claim` au tableau des routes ; compléter le schéma de `memorial_tributes` avec `page_id` et la contrainte ; ajouter une entrée de session ; si le fichier dépasse 750 lignes, déplacer la plus ancienne des deux sessions vers `docs/SESSIONS.md`.

Dans `docs/acquisition/specs.md`, section PP-2 : consigner les trois obstacles trouvés à l'audit et la façon dont ils ont été traités, en particulier l'exemption du déclencheur d'entrées.

- [ ] **Step 6: Vérifications finales**

Arrêter le serveur de dev, puis `npm run build`, `npm test`, `npx tsc --noEmit`.

- [ ] **Step 7: Commit**

```bash
git add -A
git commit -m "feat(public-pages): rattrapage au tableau de bord, quota, documentation"
```

---

## Ordre et charge

| Étape | Charge | Dépend de |
|---|---|---|
| Tâche 1, migration et RPC | M | rien, mais bloque tout le reste tant qu'elle n'est pas appliquée |
| Tâche 2, hommages | S | tâche 1 |
| Tâche 3, route de réclamation | S | tâche 1 |
| Tâche 4, copie | S | rien |
| Tâche 5, page publique | M | tâches 2, 3, 4 |
| Tâche 6, tableau de bord et docs | M | tâche 5 |

## Ce que PP-2 ne fait pas

- **La porte B**, pages d'animaux vivants : PP-4, conditionné à une mesure. Le `kind = 'living'` est porté par la RPC, mais aucune route ne crée de page vivante.
- **Une interface de modération nouvelle** : les hommages réclamés arrivent dans l'onglet Hommages existant de la page animal.
- **Un email au propriétaire** quand un hommage est déposé sur une page non réclamée : il n'y a pas de propriétaire. L'email existant reste réservé aux animaux.
